<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.PokemonApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Color primario -->
        <item name="colorPrimary">@color/pokemon_yellow</item>
        <!-- Color secundario -->
        <item name="colorSecondary">@color/pokemon_red</item>
        <!-- Color de fondo -->
        <item name="android:colorBackground">@color/pokemon_white</item>
        <!-- Color de texto -->
        <item name="android:textColor">@color/text_color</item>
    </style>
</resources>